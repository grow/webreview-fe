<form class="form" {{action "updateProject" on="submit"}}>

  <div class="form-field">
    <label>Name</label>
    {{input type="text" value=project.nickname class="form-control" autofocus="true"}}
  </div>

  <div class="form-field">
    <label>Description</label>
    {{textarea value=project.description class="form-control"}}
  </div>

  <div class="form-field">
    <label>Repository URL</label>
    {{input type="text" value=project.git_url class="form-control form-control--max"}}
  </div>

  <div class="form-field form-field-last">
    <button type="submit" class="btn btn--primary">Save</button>
  </div>

  {{error-list errors=errors}}
</form>
