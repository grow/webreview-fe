<table class="table table--min">
  <thead>
    <tr>
      <td>{{caption}}</td>
      <td>Role</td>
      <td></td>
    </tr>
  </thead>
  {{#each memberships as |membership|}}
    <tr>
      <td>
        {{#if membership.user}}
          {{owner-chip owner=membership.user}}
        {{else}}
          Everyone in {{membership.domain}}
        {{/if}}
      </td>
      <td>
        {{#if membership.user}}
          {{role-select
              team=team
              membership=membership
              project=project
              selectedRole=membership.role
              roles=userRoles}}
        {{else}}
          {{role-select
              team=team
              membership=membership
              project=project
              selectedRole=membership.role
              roles=domainRoles}}
        {{/if}}
      </td>
      <td>
        <div class="form-control-x" {{action "deleteMembership" team membership}}>✕</div>
      </td>
    </tr>
  {{/each}}
</table>
