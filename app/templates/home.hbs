<div class="box">
  <h1>Your projects</h1>
  <table>
    {{#each project in model}}
      <tr>
        <td>{{#link-to 'project.builds' project.owner.nickname project.nickname}}{{project.nickname}}{{/link-to}}</td>
      </tr>
    {{/each}}
  </table>
</div>
